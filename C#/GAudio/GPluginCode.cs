using System;

namespace gaudio
{
    public enum GPluginCode
    {
        AUDIO_PLUGIN_DECODER = 1,
        AUDIO_PLUGIN_EFFECT  = 2,
        AUDIO_PLUGIN_DRIVER  = 3,
        AUDIO_PLUGIN_RECORDER
    }
}